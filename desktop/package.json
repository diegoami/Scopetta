{
  "name": "scopetta-desktop",
  "version": "1.0.1",
  "private": true,
  "description": "Tauri wrapper around public/ for the Scopetta desktop build",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build --no-bundle"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.5"
  }
}

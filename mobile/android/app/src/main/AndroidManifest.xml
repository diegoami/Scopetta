<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The settings and the last hundred smazzate live in WebView
         localStorage, and SPEC.md and the release notes promise that nothing
         leaves the device. `allowBackup="false"` turns cloud backup off; the two
         rule files exclude the same data from device-to-device migration, which
         on Android 12+ (this app targets 36) some manufacturers do not disable
         from `allowBackup` alone. `data_extraction_rules.xml` is the Android 12
         format, `backup_rules.xml` the older one; each excludes every domain,
         credential-protected and device-protected. -->
    <application
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <!-- The app makes no network requests: everything it draws with is in
         assets/public/, which is the point of the fonts pass in
         tools/check_ui.mjs. This permission is still here because Capacitor
         does not load the page from file:// — it serves it to the WebView over
         an intercepted http://localhost origin, and a WebView will not load an
         http URL without it. ANDROID.md §2 and §7: dropping it can only be
         verified by installing the result, so it waits for a device. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>

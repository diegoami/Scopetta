<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12 (API 31) and higher.

  `android:allowBackup="false"` disables cloud backup, but on devices from some
  manufacturers it does not disable device-to-device migration. This is the
  attribute that does, so the app's data is excluded from both here explicitly
  — every credential-protected domain and its four device-protected
  counterparts. The WebView localStorage that holds the settings and the
  smazzate history is app-private data; none of it belongs on another device or
  in a backup.

  `path="."` is the whole domain, and it is required by Android's resource
  schema even though a generic XML parser accepts the file without it.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
        <exclude domain="device_root" path="." />
        <exclude domain="device_file" path="." />
        <exclude domain="device_database" path="." />
        <exclude domain="device_sharedpref" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
        <exclude domain="device_root" path="." />
        <exclude domain="device_file" path="." />
        <exclude domain="device_database" path="." />
        <exclude domain="device_sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>

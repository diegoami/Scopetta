{
  "name": "scopetta-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper that packages public/ as an Android APK",
  "scripts": {
    "add": "cap add android",
    "sync": "cap sync android",
    "open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
